package layeredLayoutTest;

/*Generated by MPS */

import jetbrains.mps.graphLayout.layeredLayout.ILayerer;
import jetbrains.mps.graphLayout.layeredLayout.NodeLayers;
import jetbrains.mps.graphLayout.graph.Graph;
import junit.framework.Assert;

public class LayererProxy implements ILayerer {
  private ILayerer myLayerer;

  public LayererProxy(ILayerer layerer) {
    myLayerer = layerer;
  }

  public NodeLayers computeLayers(Graph graph) throws IllegalArgumentException {
    NodeLayers layers = myLayerer.computeLayers(graph);
    Assert.assertFalse(CheckLayers.hasBadEdges(graph, layers));
    return layers;
  }
}
